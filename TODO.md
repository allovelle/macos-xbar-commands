* Fix metadata tags
* Add UI variables for resize-dock
* Add smol, med, big drop-down opts to resize-dock
* Fix how often they run (why on earth 1m?)
* Add resize-dock status to get the integer number of the dock height
* Finish toggle dark mode
* Ensure all installs from CLI work as expected
* Rewrite bespoke OSA commands as OSA scripts
* Refactor out the make_executable/etc commands into lib
* Read from JSON input files for inputs


rename them to macos-*
justfile
